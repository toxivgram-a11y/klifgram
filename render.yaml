services:
  - type: web
    name: klifgram-depot
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: ADMIN_TOKEN
        generateValue: true
      - key: NODE_VERSION
        value: 22
